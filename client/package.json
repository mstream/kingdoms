{
  "name": "kingdoms-client",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "rimraf": "3.0.2",
    "webpack": "4.41.6",
    "webpack-cli": "3.3.11",
    "webpack-dev-server": "3.10.3"
  },
  "scripts": {
    "build": "webpack --mode production",
    "deploy": "npm run build && aws s3 cp --recursive --acl public-read dist/ s3://www.maciej-laciak.com/",
    "prebuild": "rimraf dist",
    "start": "webpack-dev-server --open --mode development",
    "test": "jest --coverage"
  }
}
