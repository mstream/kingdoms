// @flow

import {
    connectProps,
} from './props';
import {
    Component,
} from './component';

export {
    testId as attackViewCityListComponentTestId,
} from './component';
export const AttackViewCityListComponent = connectProps(
    Component,
);
