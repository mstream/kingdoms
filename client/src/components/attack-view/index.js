// @flow

import { connectProps } from './props';
import { Component } from './component';

export { testId as attackViewComponentTestId } from './component';
export const AttackViewComponent = connectProps(Component);
