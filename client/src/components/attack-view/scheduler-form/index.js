// @flow

import {
    connectProps,
} from './props';
import {
    Component,
} from './component';

export {
    testId as attackViewSchedulerFormComponentTestId,
} from './component';
export const AttackViewSchedulerFormComponent = connectProps(
    Component,
);
