// @flow

import {
    connectProps,
} from './props';
import {
    Component,
} from './component';

export {
    testId as cityHeaderComponentTestId,
} from './component';
export const CityHeaderComponent = connectProps(
    Component,
);
