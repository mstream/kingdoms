// @flow

import {
    connectProps,
} from './props';
import {
    Component,
} from './component';

export {
    testId as cityOverviewComponentTestId,
} from './component';
export const CityOverviewComponent = connectProps(
    Component,
);
