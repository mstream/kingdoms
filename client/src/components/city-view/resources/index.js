// @flow

import {
    connectProps,
} from './props';
import {
    Component,
} from './component';

export {
    testId as cityResourcesComponentTestId,
} from './component';
export const CityResourcesComponent = connectProps(
    Component,
);
