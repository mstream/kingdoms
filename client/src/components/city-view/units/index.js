// @flow

import {
    connectProps,
} from './props';
import {
    Component,
} from './component';

export {
    testId as cityUnitsComponentTestId,
} from './component';
export const CityUnitsComponent = connectProps(
    Component,
);
