// @flow

import {
    connectProps,
} from './props';
import {
    Component,
} from './component';

export {
    testId as gameStartComponentTestId,
} from './component';
export const GameStartComponent = connectProps(
    Component,
);
