// @flow

import {
    connectProps,
} from './props';
import {
    Component,
} from './component';

export {
    testId as worldMapComponentTestId,
} from './component';
export const WorldMapComponent = connectProps(
    Component,
);
