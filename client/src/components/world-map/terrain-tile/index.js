// @flow

import {
    connectProps,
} from './props';
import {
    Component,
} from './component';

export {
    testId as terrainTileComponentTestId,
} from './component';
export const TerrainTileComponent = connectProps(
    Component,
);
