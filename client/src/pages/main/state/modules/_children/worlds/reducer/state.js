// @flow

import type {
    ClientStateWorlds,
} from './types';

export const emptyWorldsState: ClientStateWorlds = {
    isLoading: false,
    items    : [],
};

export const initialWorldsState: ClientStateWorlds = {
    isLoading: false,
    items    : [],
};
