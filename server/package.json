{
  "name": "kingdoms-server",
  "version": "1.0.0",
  "scripts": {
    "build": "webpack-cli",
    "clean": "rimraf .aws-sam .vscode",
    "deploy": "sam validate && npm run build && sam deploy",
    "destroy": "aws cloudformation delete-stack --stack-name kingdoms",
    "prebuild": "rimraf .aws-sam .vscode",
    "test": "jest"
  },
  "devDependencies": {
    "rimraf": "3.0.2",
    "webpack": "4.41.6",
    "webpack-cli": "3.3.11"
  }
}
