// @flow

import type {
    Config,
} from './types';

export const emptyConfig: Config = {
    clientId              : ``,
    region                : ``,
    resetStateFunctionName: ``,
    userPoolId            : ``,
};
