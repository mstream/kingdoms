// @flow

import type {
    Config,
} from './types';

export const emptyConfig: Config = {
    clientId                : ``,
    destroyWorldFunctionName: ``,
    region                  : ``,
    resetStateFunctionName  : ``,
    userPoolId              : ``,
};
